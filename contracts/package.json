{
  "name": "@runsheet/contracts",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "scripts": {
    "build": "tsc -b tsconfig.build.json",
    "generate": "tsc -b tsconfig.build.json && node --experimental-strip-types scripts/generate.ts"
  },
  "dependencies": {
    "@runsheet/kernel": "workspace:*",
    "zod": "^4.0.0",
    "@runsheet/eventstore": "workspace:*"
  }
}
