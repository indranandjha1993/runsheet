packages:
  - apps/*
  - packages/*
  - services/*
  - contracts
  - api
  - spec

# Vite's bundler ships a native binary that its install script fetches. Nothing else in the
# tree is allowed to run install scripts.
allowBuilds:
  esbuild: true
